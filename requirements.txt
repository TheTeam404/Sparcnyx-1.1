# GUI Framework
PyQt5>=5.15.0
# PyQt5-sip>=12.0 # Often installed as a dependency of PyQt5
# PyQt5-Qt5>=5.15.2 # Qt binaries, also often a dependency

# Core numerical and scientific libraries
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
scikit-learn>=1.0.0

# Plotting library
matplotlib>=3.4.0

# Optional: For advanced wavelet transforms (if conceptual_wavelet_denoise is fully implemented)
# pywavelets>=1.1.0

# Optional: For online NIST database queries (if that feature is built)
# astroquery>=0.4.0

# Optional: For image handling beyond Matplotlib (e.g., saving maps in various formats, icon creation)
# Pillow>=8.4.0

# Optional: For natural sorting of filenames (e.g., in file import dialogs)
# natsort>=8.0.0

# --- Developer/Testing Tools (Typically in a separate requirements-dev.txt) ---
# pytest>=6.0.0
# pytest-qt>=4.0.0
# mypy>=0.900 # For static type checking
# flake8>=3.9.0 # For linting
# black>=21.0b0 # For code formatting